/* El "include" va a incluir la libreria. 
El ".h" significa que es un header/archivo de cabecera que va a funcionar como una libreria en C.
El "stdio.h" significa standar input output header. 
*/
#include<stdio.h>

//La funcion main es esencial, utilizando las llaves {} y el return 0; antes de la ultima llave, que indica que el programa ha terminado.

//int = integer (entera)
// \n indica un salto de línea. (se hace con Alt Gr + la tecla de "?"") 
int main(){
    printf("Hola Mundo XD");
    return 0;
}

