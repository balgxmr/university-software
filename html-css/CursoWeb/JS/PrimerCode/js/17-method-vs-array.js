// Diferencia entre método y función
// La diferencia radica más en el contexto en el que se usan

const numero1 = 20;
const numero2 = "20";

// parseInt es una función
console.log(parseInt(numero2));
// Esto es un método, "variable.método"
console.log(numero1.toString());
